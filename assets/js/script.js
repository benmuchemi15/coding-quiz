const startButton = document.getElementById("ques1")

startButton.addEventListener("click", startQuiz)

function startQuiz() {

}


var quiz = [
    questions: [
        q: "Commonly used data types DO NOT Include:",
        options: ["strings", "boolean", "alerts", "numbers"]
        answer: 1
    ]
]

function startQuestion() {

}

